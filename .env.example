# Copy to .env and fill secrets. Used by compose.yaml (backend env_file + db password).
POSTGRES_PASSWORD="postgres"
DATABASE_URL="postgresql://postgres:postgres@db:5432/school_crm"
JWT_SECRET="change-me-in-prod"
JWT_EXPIRES_IN="8h"
CORS_ORIGIN="https://your-frontend.pages.dev"
PORT=4000
